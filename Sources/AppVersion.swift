import Foundation

enum AppVersion {
    static let current = "3.0.0"
    static let build = "20260217"
    static let improvements = "🚀 Vox AI Engine - Transcrição mais inteligente e precisa"

    static var full: String {
        "\(current) (\(build))"
    }
}
